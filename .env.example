# Copy to .env for `docker compose up`. .env is gitignored; this template is committed.
# Production must supply strong, externally-managed values (never these defaults).
POSTGRES_PASSWORD=nexus
JWT_SECRET=change-me-to-a-long-random-secret
